<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Players</title>
    <style>
      body {
        margin: 0;
        font-family: "Georgia", serif;
        background-color: gray;
        color: #333;
      }

      nav {
        background: linear-gradient(to right, #1f1f1f, #333);
        color: white;
        padding: 10px 20px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }

      nav h1 {
        margin: 0;
        font-size: 1.8rem;
        letter-spacing: 2px;
      }

      nav ul {
        list-style: none;
        display: flex;
        gap: 20px;
        margin: 0;
        padding: 0;
      }

      nav ul li {
        margin: 0;
      }

      nav ul li a {
        color: white;
        text-decoration: none;
        padding: 8px 15px;
        font-size: 1rem;
        transition: background-color 0.3s ease, color 0.3s ease;
        border: 2px solid transparent;
        border-radius: 5px;
      }

      nav ul li a:hover {
        background-color: #f5f5f5;
        color: #333;
        border-color: #333;
      }

      main {
        padding: 20px;
        margin-top: 20px;
      }
      main ul {
        list-style: none;
        gap: 20px;
        margin: 0;
        padding: 0;
      }
      main li {
        background: white;
        margin: 15px 0;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        transition: transform 0.2s ease, box-shadow 0.2s ease;
      }

      main li:hover {
        transform: translateY(-5px);
        box-shadow: 0 6px 10px rgba(0, 0, 0, 0.2);
      }
    </style>
  </head>
  <body>
    <nav>
      <h1>King's Gambit</h1>
      <ul>
        <li><a href="/indexRoute">Home</a></li>
        <li><a href="/posts">About</a></li>
        <li><a href="/users">Users</a></li>
      </ul>
    </nav>
    <main>
      <ul>
        <% foundUser.forEach(foundUser => { %>
        <li>
          <br />
          <span>Name:</span><%= foundUser.name %> <br /><span>Age:</span>(<%=
          foundUser.age %>)<br />
          <span>Ranking:</span>(<%= foundUser.ranking %>)
        </li>
        <form action="/users/<%= foundUser._id %>?_method=DELETE" method="POST">
          <button
            type="submit"
            onclick="return confirm('Are you sure you want to delete this user?')"
          >
            Delete
          </button>
        </form>
        <form action="/users/<%= foundUser._id %>?_method=PATCH" method="POST">
          <label for="name-<%= foundUser._id %>">Name:</label>
          <input
            id="name-<%= foundUser._id %>"
            type="text"
            name="name"
            value="<%= foundUser.name %>"
            required
          />

          <label for="age-<%= foundUser._id %>">Age:</label>
          <input
            id="age-<%= foundUser._id %>"
            type="number"
            name="age"
            value="<%= foundUser.age %>"
            required
          />

          <label for="ranking-<%= foundUser._id %>">Ranking:</label>
          <input
            id="ranking-<%= foundUser._id %>"
            type="number"
            name="ranking"
            value="<%= foundUser.ranking %>"
            required
          />

          <!-- Update Button -->
          <button type="submit">Update</button>
        </form>
        <% }) %>
      </ul>
    </main>
  </body>
</html>
